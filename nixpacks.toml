[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx tsx scripts/generate-sitemap.ts",
  "npm run build"
]

[start]
cmd = "npx serve dist"
