## Изменения шага «О вас»

**1. Дата рождения — упростить**
- Убрать кнопку «Выбрать в календаре» и весь popover-календарь в `BirthDatePicker.tsx`. Оставить только три поля: День, Месяц, Год.
- Сделать **Год** обычным `Input type="number"` (ввод вручную), а не выпадающим списком. **Месяц** — оставить селект (так удобнее), но добавить возможность ручного ввода номера через текстовый инпут с автодополнением? — нет, по запросу пользователя «год месяц тоже можно руками ввести» → меняем и Месяц на `Input type="number"` (1–12) с подписью названия месяца под полем при валидном вводе. Если хотите оставить селект для месяца — уточните.
- Удалить разделитель «или» и связанные импорты (Popover, Calendar icon, format, Select для popover).

**2. Валидация возраста**
- Допустимый диапазон даты рождения: **01.01.1920 — сегодня минус 18 лет**.
- Если возраст < 18 → показывать ошибку под полем: «Сервис доступен с 18 лет».
- Если год < 1920 или дата в будущем → «Введите корректную дату рождения».
- Кнопка «Далее» в Step 2 блокируется, пока дата не валидна (текущая проверка `formData.birth_date` дополняется проверкой возраста).

**3. Рост и вес в одну строку**
- В `RegisterStep2.tsx` объединить блоки Вес и Рост в `grid grid-cols-2 gap-3`.

**4. Передача ошибки**
- `BirthDatePicker` получит проп `onValidityChange?: (valid: boolean) => void` или будет возвращать `undefined` в `onChange` при невалидной дате (текущее поведение) + рендерить текст ошибки внутри компонента на основе введённых значений.

### Файлы
- `src/components/BirthDatePicker.tsx` — переписать: 3 инпута (день/месяц/год), валидация 1920…сегодня-18, сообщение об ошибке, убрать popover.
- `src/components/register/RegisterStep2.tsx` — вес+рост в `grid grid-cols-2`.

### Уточнение
Месяц оставить **селектом** (как сейчас) или сделать ручным числовым вводом 1–12? По умолчанию плана — оставлю **селект**, потому что названия месяцев читаются понятнее, а пользователь явно просил руками вводить «год месяц» — день уже руками, год сделаю руками, месяц могу и руками, и селектом. Подтвердите.