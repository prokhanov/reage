Проблема: в статическом демо-снапшоте `src/data/exampleReport.json` поле `profile.last_name` пустое (`""`), поэтому на обложке и в данных пациента отображается только «Елена», без фамилии.

Что нужно сделать:
1. В файле `src/data/exampleReport.json` заменить `"last_name": ""` на `"last_name": "Иванова"`.

Последствия:
- Обложка `ReportCover` уже склеивает `first_name` + `last_name` и покажет «Елена Иванова».
- Блок «Данные пациента» и другие места, использующие полное имя, тоже подтянут фамилию.
- Никакие другие файлы править не нужно.