Правки Блока 3 (HeroBlockPortrait):

**Лого**
- Переместить лого выше — прямо над бейджем «Москва и Санкт-Петербург», в той же левой колонке (а не в отдельной полноширинной строке сверху).
- Увеличить размер до `h-20 md:h-24`.

**Виджеты вокруг лица (по референсу amoCRM)**
- Сделать карточки разного размера, без обрезки контента, не накладываться на лицо.
- BioAge — оставить как сейчас (крупная, ~scale 0.78), верх-лево, лёгкий наклон, выходит за лицо слева.
- BiomarkersWidget — вернуть к прежнему размеру (compact), правый верх, рядом с плечом, не на лице.
- SystemsWidget — компактная, левый низ.
- AIAssistantWidget — компактная, правый низ.
- Убрать жёсткие `scale-*` (они режут содержимое). Вместо `scale` использовать естественные размеры виджетов с ограничением `w-[Npx]`, чтобы контент не обрезался.
- Позиции — асимметрично, лицо (центр-верх портрета) свободно.

**Высота**
- Поднять контейнер портрета до `h-[460px] sm:h-[520px] lg:h-[600px]`, чтобы виджеты и текст влезали без обрезки.
- Левая колонка: уменьшить `gap` между блоками на 1 ступень для компактности.

**Без изменений**: тексты, кнопки, фон/градиент, StatRow над кнопками.

Файл: `src/components/landing/v2/HeroBlockPortrait.tsx`.