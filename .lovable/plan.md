В админке, в карточке пациента, заменить отображение поля «Возраст» на «Дата рождения», а возраст писать в той же строке после даты.

Что меняется:
1. `src/pages/admin/PatientProfile.tsx` — в шапке карточки пациента:
   - Label: «Возраст» → «Дата рождения»
   - Value: `37 лет` → `02.07.1988 (37 лет)`
   - Форматировать `profile.birth_date` через `date-fns` (`format(birthDate, 'dd.MM.yyyy', { locale: ru })`).

2. `src/components/admin/PatientInfoDialog.tsx` — на вкладке «Обзор» → «Личные данные»:
   - Label: «Возраст:» → «Дата рождения:»
   - Value: `37 лет` → `02.07.1988 (37 лет)`
   - Аналогичное форматирование даты.

Если `birth_date` отсутствует — показывать «Не указана» вместо возраста.

Технические детали:
- В `PatientProfile.tsx` `format` и `ru` из `date-fns` уже импортированы.
- В `PatientInfoDialog.tsx` нужно добавить импорт `format` и `ru` из `date-fns`/`date-fns/locale`.
- Функция `calculateAge` уже есть в обоих файлах, возраст вычисляется тем же способом.