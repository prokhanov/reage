Проблема понятна: глюкоза не должна «пропадать» на мобильном. Отчёт должен рендериться один раз в одинаковой PDF/A4-геометрии, а мобильный/десктоп должны отличаться только визуальным масштабом страницы.

План исправления:

1. Убрать влияние мобильного контейнера на пагинацию
   - Проверить `PagedReportPreview.tsx`, чтобы Paged.js всегда считал документ в фиксированной A4-ширине, независимо от ширины диалога/экрана.
   - Не менять HTML отчёта, `ReportDocument`, `BiomarkerCard`, parser или данные под мобильный экран.

2. Разделить «layout для Paged.js» и «visual scale для экрана»
   - Paged.js должен работать с натуральной шириной страницы `210mm`.
   - Масштабирование делать только внешним `transform: scale(...)` после завершения пагинации.
   - Контейнер под scaled preview должен получать рассчитанную ширину/высоту, но не должен менять ширину самой `.pagedjs_pages` во время расчёта страниц.

3. Убрать причины расхождения мобиль/десктоп
   - Не использовать CSS `zoom`.
   - Не использовать разные CSS-правила отчёта по viewport.
   - Проверить, что `.rl-biomarker`, `.rl-bio-head`, `.rl-bio-scale` не получают мобильных адаптаций.
   - Проверить overflow/height у shell/output, чтобы низ страницы не клипался и следующая карточка не исчезала под обрезкой.

4. Добавить контрольную проверку после фикса
   - Открыть один и тот же отчёт на mobile и desktop.
   - Сравнить количество `.pagedjs_page`.
   - Сравнить список/количество biomarker/editable элементов.
   - Отдельно проверить, что после селена в мобильном рендере присутствует карточка «Глюкоза», как на десктопе.
   - Визуально проверить скрин мобильного превью: страница заполнена по ширине, но состав страниц совпадает с десктопом.

Файлы, которые планирую трогать:
- `src/lib/reportLab/renderer/PagedReportPreview.tsx`

Файлы, которые трогать не планирую:
- parser/buildFromDb
- `ReportDocument`
- карточки биомаркеров как данные/структуру
- backend/PDF export