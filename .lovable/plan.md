В `src/lib/reportLab/renderer/ReportOverview.tsx`:

1. Удалить строку 24 — `<div className="rl-eyebrow">Общее заключение</div>`.
2. В H1 (строки 26–29) убрать вторую строку с ФИО. Оставить просто:
   ```tsx
   <h1 className="rl-h1" data-section-title="Обзор биологического состояния">
     Обзор биологического состояния
   </h1>
   ```
   Убрать `<br />` и `<span>для {fullName}</span>`. Переменные `patient` / `fullName` тоже удалить, если больше не используются.
3. Удалить строку 62 — `<div className="rl-eyebrow">Ключевой вывод</div>`. Оставить только рендер `<ProseMarkdown>` внутри `.rl-conclusion`.

Больше ничего не трогаю: метрики, текст AI-резюме, стили и колонтитулы остаются как есть.

Проверю на `/admin/report-visuals` скриншотом, что на обзорной странице теперь только H1 «Обзор биологического состояния», блок метрик и текст резюме — без надзаголовков и без ФИО в заголовке.