## Цель
Вернуть проект в состояние «Stage 4 завершён» (23 апр, 23:04). На тот момент:
- единый `snapshotRenderer.tsx` уже существовал (Web + PDF из одного snapshot),
- `Recommendations.tsx` имел двойную ветку: новый snapshot + markdown-fallback,
- БД хранила отчёты в **многострочном формате** (по одной строке на категорию + «Общее резюме» + «Данные пациента»),
- `EditReportDialog` работал на Quill,
- триггер `validate_recommendation_type` НЕ знал про тип `'snapshot'`,
- промпты в `ai_prompt_settings` содержали `global_anchor_rules` и markdown-инструкции.

## Что вы делаете сами (одной кнопкой)

В чате найдите моё сообщение от **23 апр, ~23:04** со словами «**Stage 4 завершён — готов единый snapshot-рендерер для Web и PDF**» и нажмите под ним кнопку **Revert**.

Это автоматически откатит весь код, написанный после этой точки:
- удалит `src/lib/snapshotMarkdown.ts`,
- вернёт прежний `EditReportDialog.tsx` (Quill, многострочный)
- вернёт прежний `Recommendations.tsx` (с fallback на markdown),
- вернёт `anchorParser.ts` и `anchorRenderer.tsx` как полноценные модули,
- откатит `analyze-biomarkers/index.ts` к версии Stage 3 (запись по категориям, без `type='snapshot'`).

## Что я подготовлю отдельно — обратная миграция БД

Revert кода **не трогает БД**. Поэтому я подготовлю SQL-миграцию, которая:

1. **Удалит тестовый snapshot-отчёт** — единственная запись с `type='snapshot'` (analysis `ffa1578f-...`, создана 24 апр 21:35). Без удаления старый код её просто не сможет обработать (он не знает про этот тип).

2. **Удалит индекс** `idx_recommendations_analysis_type`, добавленный миграцией от 24 апр.

3. **Восстановит триггер** `validate_recommendation_type` к виду без `'snapshot'` — только legacy-типы и категории биомаркеров. Если что-то попытается записать `type='snapshot'`, триггер выкинет ошибку (правильное поведение для откатанной версии).

4. **Восстановит промпты** в `ai_prompt_settings` — это самое деликатное:
   - вернёт удалённый ключ `global_anchor_rules` (с инструкциями про HTML-якоря `<!-- anchor:biomarker CODE -->`);
   - перезапишет `summary_system/user`, все `category_*_system/user` и `global_biomarkers_instructions` старыми markdown-ориентированными текстами.

   Тексты беру из git-снапшота миграции, которая существовала на момент Stage 4 (она хранится в репозитории и доступна через revert-кода).

## Что останется неизменным (и это нормально)

- **Колонка `recommendations.content_json`** — её мы добавили ещё в Stage 1 (до 23 апр) и она **была частью версии Stage 4**, так что трогать не нужно. Старый код просто не пишет туда ничего.
- **Edge-функция `analyze-biomarkers`** в Supabase будет автоматически переразвёрнута со старым кодом при первом же изменении после revert. Если хотите задеплоить вручную сразу — скажите.

## Технические детали миграции (для справки)

```sql
-- 1. Удаление тестового snapshot
DELETE FROM recommendations WHERE type = 'snapshot';

-- 2. Удаление индекса
DROP INDEX IF EXISTS idx_recommendations_analysis_type;

-- 3. Откат триггера к виду без 'snapshot'
CREATE OR REPLACE FUNCTION validate_recommendation_type() ...
  -- ARRAY['Данные пациента', 'Общее резюме'] || ARRAY_AGG(name) FROM biomarker_categories
  -- (без 'snapshot')

-- 4. UPDATE ai_prompt_settings SET prompt_text = '...' WHERE key IN (...)
--    + INSERT 'global_anchor_rules'
```

## Порядок действий

1. **Сначала вы делаете Revert кода** через чат (на сообщении от 23 апр 23:04).
2. **Потом я применяю миграцию БД** — её я сгенерирую как один SQL-файл и пришлю на подтверждение.
3. После применения проверяем, что Recommendations открывается на старых отчётах и при необходимости генерируется новый.

## Риски и подтверждение

- Тестовый snapshot-отчёт (1 шт.) **будет удалён безвозвратно**. Все ваши «настоящие» отчёты в старом формате (3 общих резюме, ~25 секций) **останутся нетронутыми**.
- Тексты промптов от 24 апр **перезапишутся старыми**. Если в новых промптах было что-то ценное, что хочется сохранить — скажите, я экспортирую их перед откатом в отдельный файл.

Подтвердите, что согласны:
- (а) делаете Revert кода сами,
- (б) я готовлю обратную миграцию БД,
- (в) тестовый snapshot-отчёт можно удалить,
- (г) промпты возвращаем к версии Stage 4 (или сначала экспортировать новые в архив?).
