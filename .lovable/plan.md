## Что сейчас не так

На вкладке «Моё здоровье» в диалоге расчёта стратегии — мешанина из внутренней математики формулы био-возраста, которая клиенту и даже админу не нужна в таком виде:

- Блок «Почему такие цифры» показывает 4 чипа: **Хроно**, **Health Index**, **База био**, **AI δ** — три из них это служебные термины формулы (`база = хроно + (85 − HI)·0.25`, `AI δ` — корректировка ИИ). Это нужно инженеру, не пользователю интерфейса.
- В этих чипах возрасты выводятся без `.toFixed(1)` — поэтому показывается `23` вместо `23.4` (хотя в основном блоке формат с десятичной долей уже соблюдается).
- Поле «Новый био-возраст (к публикации)» дублирует число, которое уже видно в карточке выше — отсюда ощущение «зачем это второй раз».
- Подпись «База био» и «AI δ» — нерасшифрованный жаргон.

## Что делаем (только UI диалога `StrategyPreviewDialog.tsx`, вкладка «Моё здоровье»)

1. **Упростить карточку «Почему такие цифры»**
   - Убрать ряд чипов «Хроно / Health Index / База био / AI δ» целиком.
   - Оставить только текстовые причины (`exp.drivers` — это и так читаемые предложения, сгенерированные ИИ).
   - Заголовок переименовать в **«Что повлияло на расчёт»**.
   - Блок «Топ-N отклонений» оставляем — он понятный и полезный.

2. **Перестроить блок «Ключевые цифры здоровья»** — это и есть единственное место с цифрами здоровья, никаких параллельных «база/дельта»:
   - Три поля в одну строку: **Хронологический возраст**, **Биологический возраст**, **Индекс здоровья (0–100)**.
   - Под каждым полем — короткая подпись-пояснение мелким шрифтом:
     - под хроно: «по дате рождения»
     - под био: «итог расчёта — это число увидит клиент»
     - под HI: «доля показателей в норме, с учётом важности»
   - Убрать из лейбла био-возраста скобки «(к публикации)» — это и так очевидно из подписи.
   - В режиме `edit` подписи те же — никакой разницы в терминологии между preview и edit.

3. **Формат чисел — единый, 1 знак после запятой**
   - В блоке «Индекс здоровья — разбивка» бэйдж `{exp.health_index.value}/100` — оставить как целое (HI — целое по дизайну).
   - Возрасты везде в этом диалоге форматировать через хелпер `fmtAge(n) = Number(n).toFixed(1)` — это касается значений по умолчанию в инпутах (сейчас `String(data.current_bio_age)` может дать `23` вместо `23.0`).
   - Инпуты возрастов: `step="0.1"`, плейсхолдер с примером `23.4`.

4. **Убрать карточку «Индекс здоровья — разбивка» из вкладки «Моё здоровье»**, если её содержимое уже целиком повторяет `drivers` (по факту — да: оба массива описывают одно и то же). Если у `exp.health_index.breakdown` бывают строки, которых нет в `drivers`, — оставляем, но переименовываем заголовок в **«Как сложился индекс здоровья»** и убираем бэйдж `/100` (HI уже виден в поле выше).

## Что НЕ трогаем

- Логику расчёта в `finalize-analysis` и `compute-health-strategy` (формула, slope, штрафы — всё остаётся).
- Вкладку «Стратегия здоровья».
- Сохранение/публикацию — поля БД те же.

## Технические детали

Файл: `src/components/health-strategy/StrategyPreviewDialog.tsx`

- Удалить блок `<div className="grid grid-cols-2 md:grid-cols-4 gap-2 pt-2 text-xs">` со Stat-чипами (строки ~225–230).
- Заменить заголовок «Почему такие цифры» → «Что повлияло на расчёт».
- В инициализации стейтов `chrono`, `bio` (там, где `useState(String(data.current_bio_age))`) — обернуть в `Number(x).toFixed(1)`.
- Добавить под каждым `<Field>` элемент `<p className="text-[11px] text-muted-foreground mt-1">…</p>`.
- Убрать слова «(текущий)», «(к публикации)», «Новый» из лейблов — лейблы становятся **«Хронологический возраст»**, **«Биологический возраст»**, **«Индекс здоровья»**.

После approve — выполню одной правкой файла.