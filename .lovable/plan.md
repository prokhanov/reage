

## Проблема

Демо-промпт содержит плейсхолдеры, которых нет в боевой функции:
- `{previous_biomarkers}` — **не существует**. В боевой функции тренды передаются через `{trends}`
- `{biomarkers}` — работает, но формат в боевой функции включает 7-сегментные нормы и 4-tier статус (🟢🟡🟠🔴), а не просто значения
- Отсутствует `{userContext}` — контекст пациента (анамнез, жалобы, симптомы, соблюдение назначений)
- Отсутствует `{recommendations}` — предыдущие рекомендации

## Реальные плейсхолдеры из боевой функции (строки 600-606)

| Плейсхолдер | Что подставляется |
|---|---|
| `{userContext}` | Возраст, пол, рост, вес, BMI, анамнез, жалобы, симптомы, соблюдение назначений |
| `{category}` | Название категории (напр. "Энергия и восстановление") |
| `{biomarkersText}` / `{biomarkers}` | Биомаркеры с значениями, 7-сегментными нормами и 4-tier статусом |
| `{trends}` | Значения из предыдущих анализов по датам |
| `{recommendations}` | Предыдущие рекомендации по этой категории |

## План

1. **Исправить блок данных в демо-промпте** — заменить концовку промпта:
   - Убрать `{previous_biomarkers}` 
   - Добавить `{userContext}`, `{trends}`, `{recommendations}`
   - Привести к формату, совместимому с боевой функцией

2. **Обновить превью подстановки** в `PromptDemoTab` — показывать все 5 реальных плейсхолдеров с примерами данных, включая тренды и контекст пациента

Изменения только в `src/pages/admin/ReportVisualsTest.tsx`, боевая функция не затрагивается.

