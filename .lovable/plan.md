Проблема уже видна в новых логах: письмо теперь триггерится и попадает в очередь, но отправщик отклоняет его с ошибкой `missing_unsubscribe` — для app-писем обязателен `unsubscribe_token`.

План исправления:

1. В `send-verification-email` добавить тот же механизм `getOrCreateUnsubscribeToken`, который уже работает в тестовых/других письмах.
2. Перед постановкой письма подтверждения в очередь создать или переиспользовать токен для email получателя.
3. Добавить `unsubscribe_token` в payload письма подтверждения.
4. Оставить триггер строго там же: после нажатия «Далее» в регистрации.
5. После изменения развернуть только изменённую edge-функцию и проверить по логам: новая ошибка `missing_unsubscribe` должна исчезнуть, запись должна перейти из `pending` в `sent`.

Что не трогаем: домен, шаблон из админки, регистрацию, welcome/drip письма, флаг подтверждения в ЛК.