# Plan: обновление подписи на фото в /energy

## Goal
Изменить оформление надписи «Больше энергии для важных вещей» на фото в шапке ReAge Energy, чтобы она соответствовала присланному скриншоту: лёгкая, воздушная подпись в правом верхнем углу фото, без нижней плашки.

## Changes
- `src/components/landing/energy/EnergyHero.tsx`
  - Убрать текущий `figcaption`-пил внизу фото.
  - Добавить текстовый блок в правом верхнем углу фото (`absolute top-4 right-4` / `md:top-6 md:right-6`).
  - Использовать существующий `font-display` + `italic` (Fraunces → Georgia italic fallback) для элегантного рукописного/италик-ощущения без добавления нового шрифта.
  - Добавить лёгкую тень текста, чтобы подпись читалась на фото.
  - Сохранить русский текст «Больше энергии для важных вещей» и разрешить естественные переносы на узких экранах.
  - Проверить адаптивность и контраст.

## Acceptance
- Подпись находится в правом верхнем углу фото, без нижней плашки.
- Стиль близок к референсу: лёгкий, италик, без тяжёлого фона.
- Сборка проходит без ошибок, мобильная вёрстка не ломается.
