## Что показали логи

Регистрация ~19:55 МСК (`a.s.prokhanov@yandex.ru`):
- ✅ ушло приветственное письмо `welcome` (drip-серия) в 19:55:08
- ❌ письмо подтверждения `signup` НЕ отправлялось

Для сравнения прошлые регистрации (19:21, 19:10, 18:46, 18:32 МСК) слали обе: `signup` + `welcome`. Сейчас — только welcome.

## Гипотеза

Юзер создан в момент переключения `auto_confirm_email` OFF, и Supabase успел автоподтвердить email по старой конфигурации → событие `user_confirmation_requested` не возникло → хук не вызвался.

## План диагностики и починки

1. Проверю в БД, выставлен ли `auth.users.email_confirmed_at` для этого юзера прямо сейчас (через `get_users_email_confirmed`). Если стоит — это и есть причина: подтверждать письмо незачем.
2. Перепроверю текущий auth config (`auto_confirm_email` должен быть false) ещё раз через `supabase--configure_auth`, идемпотентно.
3. Триггерну ручную отправку письма подтверждения через `supabase.auth.resend({ type: 'signup' })` — для этого попрошу пользователя нажать «Отправить письмо ещё раз» в ЛК (баннер уже есть) ИЛИ дёрну принудительно вручную, если есть админский путь.
4. Альтернативно: пользователь удаляет юзера и регистрируется заново — теперь конфиг точно применён, должно прийти и `signup`, и `welcome`.

## Что НЕ трогаю

- Код фронта.
- Шаблоны писем.
- `auto_confirm_email` — он уже OFF, менять не надо.
