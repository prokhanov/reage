Повысить непрозрачность фона стеклянных виджет-карточек в Hero-блоке `src/components/landing/HeroPortrait.tsx`, оставив `backdrop-blur-xl` без изменений.

Текущее значение: `bg-card/50` (50% прозрачности) на глобальной переменной `glass` и вручную продублировано в `CompactBioAgeWidget`.

План:
1. Изменить `glass` с `bg-card/50` на `bg-card/75`.
2. Синхронно изменить фон в `CompactBioAgeWidget` (там собственный inline-стиль без использования `glass`) с `bg-card/50` на `bg-card/75`.
3. Проверить, что backdrop-blur-xl остаётся на месте.
4. Запустить `bunx tsc --noEmit` для проверки типизации.

Скорректируем при необходимости, если на темной/светлой теме карточки станут слишком плотными — но основной запрос: только непрозрачность.