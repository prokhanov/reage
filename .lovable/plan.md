## Что не так сейчас

**Лоудер в админке** (`src/components/admin/AdminCenterLoader.tsx`):
- Иконка **Lucide `Loader2`** (SVG — «разорванное кольцо» с хвостиком-дугой), крутится.
- Цвет — `text-primary` (наш акцентный синий из токенов).
- Без текста по умолчанию (label опционален).

**Лоудер на главной сейчас**:
- В `index.html` (boot): CSS-круг `border:3px solid #334155; border-top-color:#60a5fa` — сплошное серое кольцо с одним голубым сегментом. Под ним текст «Загрузка…», через 15 сек — красный блок «Приложение долго загружается… Возможно, сеть блокирует часть ресурсов» + синяя кнопка «Перезагрузить».
- В `src/pages/Index.tsx` `SectionFallback`: `border-4 border-primary border-t-transparent` — почти инверсия (сплошной цветной круг с одним прозрачным сегментом).

То есть визуально это **два разных сплошных кольца**, а в админке — **иконка Loader2 (дуга с хвостом)**. Не совпадает ни форма, ни толщина.

## Что сделать

1. **`src/pages/Index.tsx` → `SectionFallback`**: заменить CSS-кольцо на `<Loader2 className="h-8 w-8 animate-spin text-primary" />` из `lucide-react` — 1-в-1 как в `AdminCenterLoader`.

2. **`index.html` → блок `#rl-boot`**:
   - Заменить `<div>` с бордер-спиннером на **инлайн SVG иконки Loader2** (тот же path, что рендерит lucide-react), с `animation: rlspin 1s linear infinite` и `color: hsl(var(--primary))` захардкоженным hex-эквивалентом (в inline-стилях нет доступа к CSS-переменным до загрузки Tailwind — возьмём фактический hex primary из темы).
   - Убрать `<div id="rl-boot-msg">Загрузка…</div>` (текст).
   - Удалить весь блок `#rl-boot-slow` (сообщение про «долго загружается» + кнопка «Перезагрузить»).

3. **`index.html` → инлайн-скрипт boot**:
   - Убрать `setTimeout(...15000...)`, который показывал баннер, и заменить на **автоматический `location.reload()` через 20 сек**, если `#rl-boot` всё ещё в DOM (один раз за сессию — используем ту же `sessionStorage`-метку `rl-chunk-reloaded`, чтобы не уйти в цикл).
   - В ветке ChunkLoadError, если reload уже был (`already === true`) — тоже просто **тихо ждать / повторный reload не делать**, никакого текста и кнопки не показывать (боот-оверлей остаётся с крутящимся Loader2).
   - Убрать код кнопки `rl-boot-reload` (её больше нет).

## Технические детали

- Loader2 SVG (из lucide) — path: `M21 12a9 9 0 1 1-6.219-8.56` внутри `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">`.
- Primary hex для inline SVG: возьму из `src/index.css` (`--primary` в dark-теме) и подставлю как `color: #...` на wrapper.
- Никаких изменений в остальных файлах, edge functions, БД.

## Итог для пользователя

- И на главной, и в `Suspense`-фолбэках будет **та же крутящаяся иконка**, что в админке, тот же цвет, без текста.
- Никаких сообщений «долго загружается» и кнопок «Перезагрузить» — при таймауте страница молча перезагружается один раз сама.
