План исправления:

1. Добавить отдельную Telegram-логику для обновления заявок
   - Сейчас в базе есть Telegram-триггер только `AFTER INSERT` для новых записей `analysis_bookings`.
   - При нажатии «Подтвердить» существующая заявка обновляется (`UPDATE`), поэтому уведомление не вызывается.
   - Добавлю `AFTER UPDATE OF status` триггер, который будет срабатывать только когда статус реально изменился.

2. Поддержать уведомление именно для подтверждения заявки
   - Для перехода заявки в статус `scheduled` передавать `template_key = booking_scheduled`.
   - Для остальных рабочих статусов сохранить существующие ключи: `booking_received`, `booking_collected`, `booking_uploaded`.
   - В payload передавать пациента, телефон, email, дату, время, адрес, тип локации и новый статус.

3. Обновить дефолтные шаблоны в админке Telegram
   - Добавить/уточнить шаблон `booking_scheduled` как уведомление о подтверждении заявки.
   - Не перетирать уже отредактированные вручную шаблоны, если они существуют.

4. Проверить результат по базе
   - Убедиться, что появился `AFTER UPDATE` триггер для `analysis_bookings`.
   - Убедиться, что настройка `booking_status_changed` включена и шаблон для подтверждения есть.
   - После этого новые нажатия «Подтвердить»/перевод в статус «Назначен» будут отправлять сообщение в тот же Telegram-бот и попадать в журнал отправок.